<% provide(:title, @room.name) %>

 <script>
 last_message_id = 0;
 $(document).ready(function(){
 get_messages();
 new_messages_check();
$('.mes-input').keypress(function(){
$('.new_mes').removeClass('new_mes border15px')
})

}
 );
</script>

<div class="row-fluid">

<h2>
<% @room.no_pass ? @class = "free" : @class = "pass" %>
<% @size = '23'%>
<%= render 'shared/lock_icon' %>
<%= @room.name %> </h2>
</div>

<div class="row-fluid">
     <section>
        <%= render 'shared/message_form' %>
      </section>

 <ol class="messages">
  <!--  <% if @room.messages.any? %>
       <%= render @messages %>
     -->
     <% end %>
  </ol>
 </div>
</div>