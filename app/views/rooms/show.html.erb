<% provide(:title, @room.name) %>

 <script>
 var last_message_id = 0;
 $(document).ready(
 get_messages()
 );
</script>

<div class="row-fluid">
<h2><%= @room.name %> </h2>
</div>

<div class="row-fluid">
     <section>
        <%= render 'shared/message_form' %>
      </section>

 <ol class="messages">
  <!--  <% if @room.messages.any? %>
       <%= render @messages %>
     -->
     <% end %>
  </ol>
 </div>
</div>