= form_for @attachfile, :url => attach_files_path( room_id: @room.id, user_id: current_user.id), :html => { :multipart => true, :method => :post } do |f|
  %p
    = fields_for :files do |f_i|
      =f_i.file_field :file 
      =f.submit "Отправить", class: "btn", id: "attach_send_btn"