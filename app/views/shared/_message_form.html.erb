<%= form_for(@message) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
   
  <div class="span10">
    <%= f.text_field :content, placeholder: "Пишите здесь", autofocus: true %>
  
  <%= f.submit "Отправить", class: "btn btn-success"%>
  </div>
<% end %>