<% provide(:title, 'Пользователи') %>
<h2>Пользователи</h2>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
    <%= link_to user.name, user_path(user.id), class: "link_black" %>
   </li>
  <% end %>
</ul>

<%= will_paginate %>